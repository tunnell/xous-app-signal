diff -ruN -x .git -x target -x Cargo.lock -x presage-cli -x presage-store-sled -x presage-store-sqlite /tmp/presage-upstream/Cargo.toml vendor/presage/Cargo.toml
--- /tmp/presage-upstream/Cargo.toml	2026-07-06 07:24:06.054617007 -0500
+++ vendor/presage/Cargo.toml	2026-07-06 07:16:41.464462507 -0500
@@ -1,11 +1,10 @@
 [workspace]
-members = ["presage", "presage-cli", "presage-store-sqlite"]
+members = ["presage"]
 resolver = "2"
 
 [patch.crates-io]
 curve25519-dalek = { git = 'https://github.com/signalapp/curve25519-dalek', tag = 'signal-curve25519-4.1.3' }
 quickcheck_async = { git = 'https://github.com/nytopop/quickcheck_async', rev = '76b13525ca93e0725e757f841fc6ccab8bae1913' }
-libsqlite3-sys = { version = "0.36.0", git = "https://github.com/whisperfish/rusqlite", rev = '2a42b3354c9194700d08aa070f70a131a470e7dc' }
 
 # Uncomment if you're working with a local checkout of libsignal-service, but DO NOT commit!
 # [patch."https://github.com/whisperfish/libsignal-service-rs.git"]
diff -ruN -x .git -x target -x Cargo.lock -x presage-cli -x presage-store-sled -x presage-store-sqlite /tmp/presage-upstream/presage/Cargo.toml vendor/presage/presage/Cargo.toml
--- /tmp/presage-upstream/presage/Cargo.toml	2026-07-06 07:24:06.058617022 -0500
+++ vendor/presage/presage/Cargo.toml	2026-07-06 07:16:27.456461980 -0500
@@ -24,11 +24,16 @@
 serde_json = "1.0"
 sha2 = "0.10.8"
 thiserror = "1.0"
-tokio = { version = "1.48", default-features = false, features = [
-    "rt",
-    "sync",
-    "time",
-] }
+# Stage 7 (Xous fork): tokio removed from production deps per
+# docs/REPORT.md Decision 2. Replacements:
+#   - tokio::sync::Mutex      → async-lock::Mutex
+#   - tokio::task::spawn_local / tokio::spawn → executor.spawn(...).detach()
+#   - tokio::task::spawn_blocking → inline (single-threaded executor on Xous)
+#   - tokio::time::error::Elapsed → custom unit error
+# Versions taken from xous-app-signal workspace [workspace.dependencies].
+async-executor = { git = "https://github.com/smol-rs/async-executor", rev = "543403e" }
+async-lock = { git = "https://github.com/smol-rs/async-lock", rev = "29f0ff9" }
+futures-timer = { git = "https://github.com/async-rs/futures-timer", rev = "07dbe53" }
 tracing = "0.1"
 url = "2.5"
 derive_more = { version = "2.1.0", features = ["debug"] }
@@ -37,5 +42,4 @@
 [dev-dependencies]
 quickcheck = "1.0.3"
 quickcheck_async = "0.1"
-presage-store-sqlite = { path = "../presage-store-sqlite" }
 tokio = { version = "1.48.0", features = ["rt-multi-thread", "macros"] }
diff -ruN -x .git -x target -x Cargo.lock -x presage-cli -x presage-store-sled -x presage-store-sqlite /tmp/presage-upstream/presage/src/errors.rs vendor/presage/presage/src/errors.rs
--- /tmp/presage-upstream/presage/src/errors.rs	2026-07-06 07:24:06.058617022 -0500
+++ vendor/presage/presage/src/errors.rs	2026-06-09 15:38:11.228931981 -0500
@@ -61,8 +61,8 @@
     UnknownGroup,
     #[error("unknown recipient")]
     UnknownRecipient,
-    #[error("timeout: {0}")]
-    Timeout(#[from] tokio::time::error::Elapsed),
+    #[error("timeout")]
+    Timeout, // Stage 7: was `Timeout(#[from] tokio::time::error::Elapsed)`. Now a unit variant; callers map their own timeout errors via `.map_err(|_| Error::Timeout)`.
     #[error("store error: {0}")]
     Store(S),
     #[error("push challenge required (not implemented)")]
diff -ruN -x .git -x target -x Cargo.lock -x presage-cli -x presage-store-sled -x presage-store-sqlite /tmp/presage-upstream/presage/src/lib.rs vendor/presage/presage/src/lib.rs
--- /tmp/presage-upstream/presage/src/lib.rs	2026-07-06 07:24:06.058617022 -0500
+++ vendor/presage/presage/src/lib.rs	2026-06-09 15:38:11.229931981 -0500
@@ -16,3 +16,54 @@
 const USER_AGENT: &str = concat!(env!("CARGO_PKG_NAME"), "-rs-", env!("CARGO_PKG_VERSION"));
 
 pub type AvatarBytes = Vec<u8>;
+
+// Stage 7 (Xous fork): tokio-removal — see docs/REPORT.md Decision 2.
+//
+// Manager methods occasionally spawn fire-and-forget background tasks
+// (sync-message replies, sticker downloads, contact upserts). Upstream
+// uses `tokio::task::spawn_local` / `tokio::spawn`, which require an
+// ambient Tokio runtime context. We don't have one — Xous worker threads
+// run an `async_executor::LocalExecutor` instead.
+//
+// To avoid threading an executor parameter through every Manager
+// constructor, we use a thread-local: the worker thread calls
+// `presage::set_executor(...)` once at startup with a `'static`
+// LocalExecutor reference, and the Manager spawn sites route through
+// `presage::spawn_detached(...)`. The executor must outlive the program
+// (typical pattern: `Box::leak(Box::new(LocalExecutor::new()))`).
+pub mod runtime {
+    use std::cell::RefCell;
+    use std::future::Future;
+
+    use async_executor::LocalExecutor;
+
+    thread_local! {
+        static PRESAGE_EXECUTOR: RefCell<Option<&'static LocalExecutor<'static>>> =
+            const { RefCell::new(None) };
+    }
+
+    /// Install the per-thread `LocalExecutor` reference that presage's
+    /// spawn sites will use. Call once on the worker thread before any
+    /// Manager method.
+    pub fn set_executor(exec: &'static LocalExecutor<'static>) {
+        PRESAGE_EXECUTOR.with(|cell| *cell.borrow_mut() = Some(exec));
+    }
+
+    /// Spawn a `'static + !Send`-OK future as a detached background task
+    /// on the thread-local executor. Panics if `set_executor` was not
+    /// called on this thread first — that's a programmer error and
+    /// failing fast catches it.
+    pub fn spawn_detached<F>(future: F)
+    where
+        F: Future<Output = ()> + 'static,
+    {
+        PRESAGE_EXECUTOR.with(|cell| {
+            let exec = cell
+                .borrow()
+                .expect("presage::runtime::set_executor must be called on this thread first");
+            exec.spawn(future).detach();
+        });
+    }
+}
+
+pub use runtime::{set_executor, spawn_detached};
diff -ruN -x .git -x target -x Cargo.lock -x presage-cli -x presage-store-sled -x presage-store-sqlite /tmp/presage-upstream/presage/src/manager/confirmation.rs vendor/presage/presage/src/manager/confirmation.rs
--- /tmp/presage-upstream/presage/src/manager/confirmation.rs	2026-07-06 07:24:06.058617022 -0500
+++ vendor/presage/presage/src/manager/confirmation.rs	2026-06-09 15:38:11.253931982 -0500
@@ -70,9 +70,11 @@
             crate::USER_AGENT,
         );
 
-        let mut identified_websocket = identified_push_service
+        // Stage 6.1 (Xous fork): ws() returns (ws, task); spawn the task.
+        let (mut identified_websocket, ws_task) = identified_push_service
             .ws("/v1/websocket/", "/v1/keepalive", &[], Some(credentials))
             .await?;
+        crate::runtime::spawn_detached(ws_task);
 
         let session = identified_websocket
             .submit_verification_code(session_id, confirmation_code.as_ref())
diff -ruN -x .git -x target -x Cargo.lock -x presage-cli -x presage-store-sled -x presage-store-sqlite /tmp/presage-upstream/presage/src/manager/registered.rs vendor/presage/presage/src/manager/registered.rs
--- /tmp/presage-upstream/presage/src/manager/registered.rs	2026-07-06 07:24:06.058617022 -0500
+++ vendor/presage/presage/src/manager/registered.rs	2026-06-09 15:38:11.256931983 -0500
@@ -44,7 +44,9 @@
 use rand::rng;
 use serde::{Deserialize, Serialize};
 use sha2::Digest;
-use tokio::sync::Mutex;
+// Stage 7: was `use tokio::sync::Mutex;`. async-lock::Mutex is a drop-in
+// replacement for the methods presage uses (lock().await held across awaits).
+use async_lock::Mutex;
 use tracing::{debug, error, info, trace, warn};
 use url::Url;
 
@@ -227,7 +229,10 @@
             Some(ws) => Ok(ws.clone()),
             None => {
                 let headers = &[("X-Signal-Receive-Stories", "false")];
-                let ws = self
+                // Stage 6.1 (Xous fork): ws() now returns (ws, task). The
+                // task drives the SignalWebSocketProcess loop and must be
+                // spawned on the local executor.
+                let (ws, ws_task) = self
                     .identified_push_service()
                     .ws(
                         "/v1/websocket/",
@@ -236,6 +241,7 @@
                         Some(self.credentials()),
                     )
                     .await?;
+                crate::runtime::spawn_detached(ws_task);
                 identified_ws.replace(ws.clone());
                 debug!("initialized identified websocket");
 
@@ -254,10 +260,11 @@
         match unidentified_ws.as_ref().filter(|ws| !ws.is_closed()) {
             Some(ws) => Ok(ws.clone()),
             None => {
-                let ws = self
+                let (ws, ws_task) = self
                     .unidentified_push_service()
                     .ws("/v1/websocket/", "/v1/keepalive", &[], None)
                     .await?;
+                crate::runtime::spawn_detached(ws_task);
                 unidentified_ws.replace(ws.clone());
                 debug!("initialized unidentified websocket");
 
@@ -266,6 +273,30 @@
         }
     }
 
+    /// Returns the close-frame code observed on the currently-cached
+    /// identified WebSocket, or `None` if no WS has been opened yet or
+    /// the current WS is still open.
+    ///
+    /// **What "currently-cached" means for retry-decision logic:** the
+    /// slot reads as the *previous* identified WS after a `replace()`
+    /// failure — `identified_websocket()` only calls `replace()` on a
+    /// successful `ws()` handshake, so if a reconnect handshake fails
+    /// (e.g. with HTTP 403), the slot still holds the just-closed WS
+    /// from before the failed reconnect attempt. That's exactly what
+    /// callers want for retry-decision logic: they see the close code
+    /// that triggered the reconnect, not zero from a brand-new WS
+    /// that never made it onto the wire.
+    ///
+    /// Added for xas issue #13 (Bug B): after `receive_messages()`
+    /// returns a transport error, the worker reads this to distinguish
+    /// `4401 "Reauthentication required"` (apply a settling delay then
+    /// re-test; emit `Event::SignalAuthExpired` after N terminal 403s)
+    /// from `1001` idle closes and `4409` (issue #1 Bug A).
+    pub async fn last_identified_close_code(&self) -> Option<u16> {
+        let ws_guard = self.state.identified_websocket.lock().await;
+        ws_guard.as_ref().and_then(|ws| ws.last_close_code())
+    }
+
     /// Request the primary device to encrypt & send all of its contacts.
     ///
     /// **Note**: If successful, the contacts are not yet received and stored, but will only be
@@ -693,7 +724,7 @@
                                                 let mut message_sender =
                                                     state.message_sender.clone();
                                                 let aci = state.service_ids.aci();
-                                                tokio::task::spawn_local(async move {
+                                                crate::runtime::spawn_detached(async move {
                                                     let result = message_sender
                                                     .send_contact_details(
                                                         &ServiceId::Aci(aci),
@@ -724,7 +755,7 @@
                                             RequestType::Keys => {
                                                 let mut message_sender =
                                                     state.message_sender.clone();
-                                                tokio::task::spawn_local(async move {
+                                                crate::runtime::spawn_detached(async move {
                                                     let result = message_sender.send_sync_message(SyncMessage {
                                                         keys: Some(libsignal_service::content::sync_message::Keys {
                                                             master: Some(state.master_key.inner.to_vec()),
@@ -743,7 +774,7 @@
                                                 warn!("storing blocked user is not implemented yet! we will not report blocked users to the device requesting the sync.");
                                                 let mut message_sender =
                                                     state.message_sender.clone();
-                                                tokio::task::spawn_local(async move {
+                                                crate::runtime::spawn_detached(async move {
                                                     let result = message_sender.send_sync_message(SyncMessage {
                                                     blocked: Some(libsignal_service::content::sync_message::Blocked {
                                                         numbers: vec![],
@@ -813,7 +844,7 @@
                                                     let operation = operation.clone();
 
                                                     // download stickers in the background
-                                                    tokio::spawn(async move {
+                                                    crate::runtime::spawn_detached(async move {
                                                         match download_sticker_pack(
                                                             store,
                                                             unidentified_websocket,
@@ -1248,18 +1279,16 @@
 
         let key: [u8; 64] = attachment_pointer.key().try_into()?;
 
-        // Offload decryption of large attachments to another thread.
-        // Chose arbitrary threshold here.
-        const DECRYPT_IN_THREAD_THRESHOLD: usize = 100 * 1024;
-        if ciphertext.len() > DECRYPT_IN_THREAD_THRESHOLD {
-            ciphertext = tokio::task::spawn_blocking(move || {
-                decrypt_in_place(key, &mut ciphertext).map(|_| ciphertext)
-            })
-            .await
-            .expect("decryption in another thread")?;
-        } else {
-            decrypt_in_place(key, &mut ciphertext)?;
-        };
+        // Stage 7 (Xous fork): inlined the decrypt. Upstream uses
+        // `tokio::task::spawn_blocking` to offload large attachment
+        // decrypts to a blocking-threadpool. On Xous we run on a
+        // single-threaded LocalExecutor; spawn_blocking would need a
+        // separate worker thread plus a channel — overkill for a
+        // CPU-bound op that doesn't yield. Inline keeps the code simple
+        // and correct; if profiling shows the receive loop is starved
+        // by big attachment decrypts, revisit by adding a `blocking`
+        // crate-style helper. Per docs/REPORT.md Decision 2.
+        decrypt_in_place(key, &mut ciphertext)?;
 
         if let Some(len) = plaintext_len {
             if len < ciphertext.len() {
@@ -1405,8 +1434,15 @@
             self.state
                 .service_configuration()
                 .unidentified_sender_trust_roots,
+            // service_id_string() emits "<uuid>" for ACI and
+            // "PNI:<uuid>" for PNI. libsignal_service::cipher reparses
+            // local_address.name() via parse_from_service_id_string()
+            // to derive local_service — a bare UUID is treated as ACI
+            // by default, so a PNI cipher built from pni.to_string()
+            // misclassifies itself and rejects PNI-addressed envelopes
+            // with "mismatching destination service id".
             ProtocolAddress::new(
-                self.state.data.service_ids.aci.to_string(),
+                self.state.data.service_ids.aci().service_id_string(),
                 self.state.device_id(),
             ),
         )
@@ -1419,7 +1455,7 @@
                 .service_configuration()
                 .unidentified_sender_trust_roots,
             ProtocolAddress::new(
-                self.state.data.service_ids.pni.to_string(),
+                self.state.data.service_ids.pni().service_id_string(),
                 self.state.device_id(),
             ),
         )
@@ -1704,7 +1740,7 @@
                 let store_inner = store.clone();
                 let websocket_inner = identified_websocket.clone();
                 let data_message_inner = data_message.clone();
-                tokio::spawn(async move {
+                crate::runtime::spawn_detached(async move {
                     if let Err(error) = upsert_contact_from_profile(
                         store_inner,
                         websocket_inner,
diff -ruN -x .git -x target -x Cargo.lock -x presage-cli -x presage-store-sled -x presage-store-sqlite /tmp/presage-upstream/presage/src/manager/registration.rs vendor/presage/presage/src/manager/registration.rs
--- /tmp/presage-upstream/presage/src/manager/registration.rs	2026-07-06 07:24:06.058617022 -0500
+++ vendor/presage/presage/src/manager/registration.rs	2026-06-09 15:38:11.257931983 -0500
@@ -87,9 +87,11 @@
 
         let mut unidentified_push_service =
             PushService::new(signal_servers, None, crate::USER_AGENT);
-        let mut unidentified_websocket = unidentified_push_service
+        // Stage 6.1 (Xous fork): ws() returns (ws, task); spawn the task.
+        let (mut unidentified_websocket, ws_task) = unidentified_push_service
             .ws("/v1/websocket/", "/v1/keepalive", &[], None)
             .await?;
+        crate::runtime::spawn_detached(ws_task);
 
         trace!("creating registration verification session");
 
